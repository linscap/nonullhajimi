{
  "name": "gemini-ant-vercel",
  "version": "1.0.0",
  "description": "Vercel deployment for Gemini API proxy",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
